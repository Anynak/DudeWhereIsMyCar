spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://postgres:5432/dudeWhereIsMyCar
    password: '1111'
  web:
    locale: en_US
  security:
    user:
      name: user
      password: '1111'
  jpa:
    properties:
      hibernate:
        format_sql: 'true'
    show-sql: 'true'

  kafka:
    producer:
      bootstrap-servers: broker:29092
    consumer:
      bootstrap-servers: broker:29092


server:
  servlet:
    contextPath: /api
logging:
  level:
    org:
      springframework:
        web: TRACE
